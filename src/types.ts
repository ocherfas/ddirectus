export interface DirectusTypeOptions {
    url: string,
    version: string,
    typeName: string,
    exportPath: string
}

export interface DirectusType {
    directusType: string,
    directusVersion: string
}